import 'package:flutter/material.dart';

class Scareen_Bio extends StatelessWidget {
  const Scareen_Bio({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title:const Text(
            'Bio',
          style: TextStyle(

          ),
        ),
      ),
    );
  }
}
